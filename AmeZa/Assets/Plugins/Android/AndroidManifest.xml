<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.unity3d.player"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="preferExternal">
  <supports-screens
      android:smallScreens="true"
      android:normalScreens="true"
      android:largeScreens="true"
      android:xlargeScreens="true"
      android:anyDensity="true"/>

  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

  <!-- CAFE BAZAAR = com.farsitel.bazaar.permission.PAY_THROUGH_BAZAAR - MYKET = ir.mservices.market.BILLING -->
  <uses-permission android:name="SX_MARKET_PERMISSION" />	

  <application android:theme="@android:style/Theme.NoTitleBar.Fullscreen" android:icon="@drawable/app_icon" android:label="@string/app_name" android:usesCleartextTraffic="true">

    <!-- UNITY PLAYER -->
    <activity android:name="com.unity3d.player.UnityPlayerActivity" android:label="@string/app_name">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
    </activity>

    <!-- CAFE BAZAAR = bazaar.BazaarIabService - MYKET = myket.MyketIabService -->
    <meta-data android:name="billing.service" android:value="SX_MARKET_SERVICE" />
	
	<!-- CAFE BAZAAR = com.bazaar.BazaarIABProxyActivity - MYKET = com.myket.MyketIABProxyActivity -->
    <activity android:name="SX_MARKET_ACTIVITY" android:theme="@android:style/Theme.Translucent.NoTitleBar.Fullscreen" />
    <!-- <activity android:name="SX_MARKET_ACTIVITY" android:theme="@android:style/Theme.NoTitleBar.Fullscreen" android:screenOrientation="portrait" android:configChanges="orientation|keyboardHidden"/> -->

  </application>
</manifest>
